// criando um objeto literal usando a estrutura par chave e valor

const pessoa = {
    nome: 'Bruno Alejandro',
    idade: 22
}

// acessando as propriedades do objeto
console.log(pessoa.nome);
console.log(pessoa.idade);
console.log(pessoa)